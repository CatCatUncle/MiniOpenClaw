# Typer CLI Demo

## Install

```bash
uv sync
```

## Run

One-shot mode:

```bash
uv run python demo.py -m "hello"
```

Interactive mode:

```bash
uv run python demo.py
```

Show internal logs:

```bash
uv run python demo.py --logs
```
